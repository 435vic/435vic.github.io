
<script>
    const TECHNOLOGIES: [string, string][] = [
        ['cpp', 'C++'],
        ['python', 'Python'],
        ['ts', 'Javascript/Typescript'],
        ['rust', 'Rust'],
        ['nodejs', 'NodeJS'],
        ['react', 'React'],
        ['linux', 'UNIX/Linux'],
        ['docker', 'Docker'],
        ['git', 'Git'],
    ];

    const techlist = document.querySelector('.techlist')!;

    TECHNOLOGIES.forEach(([id, _name]) => {
        const tech = document.createElement('div');
        tech.classList.add('tech');
        tech.id = id;
        tech.style.backgroundImage = `url("/assets/technologies/${id}.svg")`;
        techlist.appendChild(tech);
    });
</script>

<div class="techlist">
</div>

<style>
    .techlist {
        display: flex;
        gap: 1rem;
        justify-content: center;
        margin: 0 auto;
        flex-grow: 1;
        flex-wrap: wrap;
    }

    .techlist :global(.tech) {
        width: 4rem;
        height: 4rem;
        border-radius: 0.5rem;
        -webkit-backdrop-filter: blur(10px) saturate(180%);
        backdrop-filter: blur(10px) saturate(180%);
        background-color: rgba(85, 84, 90, 0.482);
        background-size: 3rem;
        background-repeat: no-repeat;
        background-position: center;
        transition: transform 0.2s ease, margin 0.9s ease; 
    }
    
    @media (min-width: 480px) {
        .techlist {
            flex-wrap: nowrap;
        }

        .techlist :global(.tech.active),
        .techlist :global(.tech:hover) {
            transform: scale(1.2);
            margin: 0 1rem;
        }
    }
</style>
